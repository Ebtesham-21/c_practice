#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a1, an, n;
    printf("enter first num, last num and total: ");
    //scanf can just take one data at a time
    scanf("%d %d %d", &a1, &an, &n);

    return 0;
}
